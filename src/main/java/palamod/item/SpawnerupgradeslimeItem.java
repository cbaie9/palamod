package palamod.item;

import net.minecraft.world.item.Item;

public class SpawnerupgradeslimeItem extends Item {
	public SpawnerupgradeslimeItem(Item.Properties properties) {
		super(properties.stacksTo(1));
	}
}